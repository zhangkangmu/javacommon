案例1需求：
	1. 提供****.html页面，页面中有一个省份 下拉列表
	2. 当 页面加载完成后 发送ajax请求，加载所有省份

案例2需求：
    1.点击注册后,发请求保存数据库,
    2.数据库里维护了一个是否激活的字段,默认是N未激活状态,激活就是Y,
    3.数据库 保存成功后,发送一条短信,短信里包含:请求连接+username+uuid
    4.uuid是随机生成的32位数字(工具类随机生成),存在redis缓冲区里
    5.激活连接一连接就去数据库将username的激活字段改成Y

    注意:不要打开本地的redis服务端,不然会报错