<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hong.mm.dao.IndustryDao">
    <select id="addQuestionQueryCompany2Industry" resultType="industry" parameterType="int">
    SELECT id,name FROM t_industry i WHERE id IN
    (SELECT industry_id FROM tr_company_industry WHERE company_id=#{companyID})
</select>
</mapper>